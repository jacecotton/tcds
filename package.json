{
  "name": "@txch/tcds",
  "version": "3.0.0",
  "description": "Texas Children's Design System",
  "main": "index.js",
  "type": "module",
  "workspaces": [
    "packages/tokens",
    "packages/foundation"
  ],
  "scripts": {
    "build:tokens": "npm run build --workspace=packages/tokens",
    "build:foundation": "npm run build --workspace=packages/foundation",
    "build": "npm run build:tokens && npm run build:foundation",
    "postinstall": "node scripts/cleanup-assets.js && node scripts/copy-assets.js && node packages/foundation/scripts/asset-config.js && npm run build",
    "preuninstall": "node scripts/cleanup-assets.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jacecotton/tcds.git"
  },
  "author": "",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/jacecotton/tcds/issues"
  },
  "homepage": "https://github.com/jacecotton/tcds#readme",
  "dependencies": {
    "@txch/foundation": "^3.0.0",
    "@txch/tokens": "^3.0.0"
  }
}
