{
  "name": "@txch/tokens",
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build:js": "node build/to-js.js",
    "build:scss": "node build/to-scss.js",
    "build": "npm run clean && npm run build:js && npm run build:scss",
    "prepare": "npm run build"
  },
  "files": [
    "dist/scss/*.scss",
    "dist/js/*.js"
  ],
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "rimraf": "^6.0.1"
  }
}
