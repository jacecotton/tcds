{
  "name": "@txch/foundation",
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && sass --load-path=../../node_modules --style=compressed --source-map src:dist/css",
    "dev": "sass --load-path=../../node_modules --style=compressed --source-map --watch src:dist/css"
  },
  "devDependencies": {
    "@txch/identity": "^3.0.0",
    "sass": "^1.89.2"
  }
}
