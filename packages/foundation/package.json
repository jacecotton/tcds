{
  "name": "@txch/foundation",
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build:config": "node ../../scripts/asset-config.js",
    "build:scss": "npm run build:config && sass --load-path=../../node_modules --style=compressed --source-map src:dist",
    "build": "npm run clean && npm run build:scss"
  },
  "devDependencies": {
    "@txch/tokens": "^3.0.0",
    "rimraf": "^6.0.1",
    "sass": "^1.89.2"
  }
}
