{
  "name": "@txch/media",
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && cpx 'src/**/*' dist"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^6.0.1"
  }
}
