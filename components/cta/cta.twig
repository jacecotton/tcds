{% set _classes = ["tcds-cta", classes|default("")]|join(" ") %}
{% set attributes = attributes|default(create_attribute()) %}
{% set attributes = attributes.addClass(_classes) %}
{%- if url -%}
{%- set attributes = attributes.setAttribute("href", url) -%}
<a {{ attributes }}>{{ text }}</a>
{%- endif -%}
