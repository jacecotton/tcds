{% set _classes = ["", classes|default("")] %}
{% set attributes = attributes|default(create_attribute()) %}
{% set attributes = attributes.addClass(_classes) %}

{% if image is defined and image is iterable %}
  {% set attributes = attributes.setAttribute(create_attribute(image)) %}
{% endif %}

{% if fit %}
  {% set attributes = attributes.setAttribute("style", "object-fit: #{fit}; width: 100%; height: 100%") %}
{% endif %}

{% set content %}
  <img {{ attributes }}>
{% endset %}

{% if caption %}
  <figure>
    {{ content }}
    <figcaption>{{ caption|raw }}</figcaption>
  </figure>
{% else %}
  {{ content }}
{% endif %}
